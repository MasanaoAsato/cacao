/**
 * Identifies one delivered catalog build: directions, artwork, fonts and
 * modules. Bump it on every such change. Screen and PDF match only within one
 * revision; older looks are neither stored nor reproduced.
 */
export const CATALOG_REVISION = "2026-09-24.2";
